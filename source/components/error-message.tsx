import { memo } from "react";



export default memo(function ErrorMessage(
	{
		message,
		hideTitle = false,
		hideBox = false
	}:{
		message:string,
		hideTitle?:boolean,
		hideBox?:boolean
	}
){

	const boxWidth = use

	return (

	);
});
